name: Release event observer

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Observed release ${{ github.event.release.tag_name }}"

